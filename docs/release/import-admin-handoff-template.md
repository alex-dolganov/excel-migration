# Cloud Admin Handoff Template

## Данные для задачи

**Проект:**  
`<название проекта>`

**Описание:**  
`<краткое описание сервиса и сценария импорта>`

**Домен:**  
`<required-domain>`

**Docker image tag:**  
`registry.i.bitrix24.ru/<project>/<service>:<tag>`

## Что приложить к задаче

- `.env` example или список обязательных env-переменных
- reference `docker-compose.yml`
- SQL/init notes для MySQL
- требования к внешним сервисам:
  - `MySQL`
  - `RabbitMQ`
  - `Nginx`
  - `OpenTelemetry OTLP`

## Важные замечания для администраторов

- использовать новый tag образа, не переиспользовать старый
- продовый `MySQL` внешний
- продовый `RabbitMQ` внешний
- Nginx в облаке внешний по отношению к приложению
- если после выкладки нужна диагностика, логи надо запрашивать отдельно

## Рекомендуемое краткое описание изменений

- оптимизация импорта без изменения бизнес-логики
- подготовка конфигурации под облачное развертывание
- образ опубликован через Harbor и прошел проверку уязвимостей
